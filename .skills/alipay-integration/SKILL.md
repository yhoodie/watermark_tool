---
name: alipay-integration
description:
  支付宝网站支付 Skill，仅支持电脑网站支付和手机网站支付；不支持 App 支付、支付宝小程序支付或其他支付方式；不适用于微信小程序和手机 App（原生 App）应用类型，此类场景应直接拒绝并引导用户查阅相应平台文档。当用户选择网站支付进行集成，使用此 Skill。
  触发关键词："AI支付"、"接支付宝"、"接个支付"、"网站支付"等。
---
# 支付宝网站支付集成 Skill

## 强制执行要点

> ⛔ 强阻塞阅读项，必须遵循，否则违规！以下为全局强制规则，**严禁**以任何理由跳过或简化：

- **[1]** 告知用户：本 Skill 主要提供支付宝支付产品集成指引和集成问题排查指引
- **[2]** 告知用户服务声明（完整内容见步骤 1.1 阻塞确认点）
- **[3]** **先定计划再动手**：执行任何操作之前，**必须先确定功能分支并打印完整的待办步骤清单**，禁止未确定计划就直接开始执行
- **[4]** 每个步骤完成后必须对照该步骤的**完成条件**自检，有遗漏须在本步骤内补齐后再继续
- **[5]** 遇到 `<BLOCKING_CONFIRMATION>` 必须暂停等待用户明确回复，确认话术必须包含"请回复'同意'或'确认'"等显式要求
- **[6]** 禁止编造沙箱配置、假数据；禁止绕过阻塞确认点
- **[7]** **禁止输出思考过程**：不得在输出中暴露"让我先..."、"我先..."、"同时..."等思考过程或决策理由，只输出最终决策结果
- **[8]** **禁止合理化跳过确认**：禁止用"用户可能希望快速完成"、"这是一个简单场景"等理由合理化跳过阻塞确认点
- **[9]** **环境就绪强制门禁**：用户确认接入后，必须立即执行 `scripts/prepare-alipay-environment.sh`。在脚本返回 `credentials_ready`，或返回 `secret_registration_required` 且三个 Secret 注册成功前，禁止读取代码示例、生成集成代码或执行其他集成操作。

---

## 功能路由

根据用户意图**先判断功能分支**，再执行对应流程：

| 用户意图 | 功能分支 | 执行流程 |
| --- | --- | --- |
| 需要集成支付产品（接入、集成、收款、加支付功能等） | **功能一** | 步骤 1.1 → 1.2 → 1.3 → 1.4 → 1.5 → 1.6 |
| 集成过程中遇到报错或问题 | **功能二** | 步骤 2.1 → 2.2/2.3 |

---

## 阻塞确认点索引

| 位置 | 触发时机 | 等待内容 |
|------|----------|----------|
| 步骤 1.1 | 产品决策后 | 用户同意服务声明并确认接入特定产品 |
| 步骤 1.6 | 集成完成后 | 用户确认是否需要进行集成校验 |

---

## 完成条件索引

| 步骤 | 标记 | 完成条件数量                 |
|------|------|------------------------|
| 步骤 1.1 产品决策 | [M-01] | 3 项                    |
| 步骤 1.2 环境就绪前置步骤 | [M-02] | 1 项 |
| 步骤 1.3 集成前置步骤 | [M-03] | 3 项                    |
| 步骤 1.4 集成代码 | [M-04] | 3 项                    |
| 步骤 1.5 集成后说明 | [M-05] | 2 项                    |
| 步骤 1.6 集成校验 | [M-06] | 1 项                    |

---

## 文档访问规范

使用 curl 获取支付宝在线文档，文档内链接（产品介绍、接入准备、接口文档等）需递归访问获取完整内容：

```bash
curl -sL "https://ideservice.alipay.com/cms/site/{文档ID}"
```

---

## 功能一：支付产品集成指引

**触发条件**：用户需要集成支付宝支付产品。

### 步骤 1.1 产品决策

**前置条件**：阅读并充分理解**强制执行要点**，完成**功能路由**，明确**文档访问规范**。

**完成条件**：
- [M-01] 已根据用户场景决策出支付产品（手机网站支付或电脑网站支付）
- [M-01] 已征得用户明确同意
- [M-01] 已按阻塞确认点输出完整服务声明


#### 产品决策规则

1. **支持的产品**：仅支持手机网站支付、电脑网站支付, **不支持小程序支付**
2. **不支持的产品**：小程序支付、订单码支付、当面付、App 支付、JSAPI 支付、预授权支付、商家扣款等，**直接返回不支持提示, 不再进行后续集成**
3. **不支持的应用类型**：若用户的应用为**微信小程序**或**手机 App（原生 App）**，本 Skill 不适用，**必须立即告知用户本 Skill 仅适用于网站场景，并引导用户查阅相应平台的官方文档，禁止继续任何集成步骤**

#### 集成原则

- **征得用户同意**：**严禁**在未征得用户同意的情况下，擅自启动支付产品的集成流程，必须先确认用户意图并获得明确同意后，方可继续执行集成步骤。
- **单一产品集成原则**：在一次指令中，未经用户许可，**严禁**同时自动启动多个支付产品的集成，必须先确认单一产品，完成该产品的完整集成流程后，方可视情况讨论下一个产品。

<BLOCKING_CONFIRMATION>

**确认点：步骤 1.1 产品决策**

在输出支付产品决策后，**必须严格按以下顺序执行并暂停等待用户回复**：

1. **输出决策结果**：告知用户推荐的支付产品及选择理由
2. **输出服务声明**（完整打印）：
   > ⚠️ **服务声明**：使用本 Skill 即表示您同意：
   > （1）使用本服务需遵守法律法规、自行审核测试并承担使用责任，我方不对使用效果、正确性担保。
   > （2）禁止在代码、大模型对话等公网透露敏感信息（密码、API Keys、私钥等）。
3. **说明确认后的环境准备操作**：告知用户确认后将自动完成支付宝环境准备（包括必要的沙箱配置和 Secret 注册），无需再次确认。
4. **显式询问确认**（必须使用如下话术）：
   > "请问您是否同意上述服务声明并确认接入【{产品名称}】？确认后我将自动完成支付宝环境准备，然后继续代码集成。**请回复'同意'或'确认'后**，我将继续后续步骤。"
5. **等待用户回复**：必须收到用户明确同意的回复后，方可进入步骤 1.2。若用户未回复、表示犹豫或拒绝，**禁止**继续任何后续步骤。

</BLOCKING_CONFIRMATION>

### 步骤 1.2 环境就绪前置步骤

**前置条件**：已完成步骤 1.1 产品决策，**并获得用户同意进入集成流程**。

#### 环境准备脚本（必须优先执行）

```bash
bash scripts/prepare-alipay-environment.sh \
  --product "{产品名称}" \
  --env-file .env
```

> 脚本标准输出仅包含最终 JSON 结果；`alipay-cli` 安装及调用诊断日志写入脚本目录下的 `alipay-environment.log`，不得将这些日志内容作为集成结果打印给用户。

> **脚本结果处理**：
> - `credentials_ready`：直接进入步骤 1.3。
> - `secret_registration_required`：**同一轮的下一个工具调用必须是** `supabase_bulk_create_secrets`，且仅使用脚本返回的 `secrets` 对象。工具调用成功后直接进入步骤 1.3；失败时停止。
> - `error` 或非零退出：输出脚本返回的错误并停止。禁止创建占位符、读取代码示例或生成代码。

> **环境就绪门禁**：只有脚本返回 `credentials_ready`，或脚本返回 `secret_registration_required` 后 `supabase_bulk_create_secrets` 明确成功，才允许进入步骤 1.3。

---

**完成条件**：
- [M-02] 脚本返回 `credentials_ready`，或 `secret_registration_required` 后 Secret 注册成功

### 步骤 1.3 集成前置步骤

**前置条件**：已完成步骤 1.2 环境就绪前置步骤，且脚本返回 `credentials_ready`，或脚本返回 `secret_registration_required` 后 Secret 注册成功。

> ⛔ **禁止跳过前置条件**：未满足环境就绪条件时，**禁止读取本步骤任何代码示例文件，禁止生成任何集成代码**。必须返回步骤 1.2 执行环境准备脚本，并在需要时注册脚本返回的 Secret。

**完成条件**：
- [M-03] 已使用 Read 工具读取 `references/code-examples/nodejs/` 下的所有代码示例；如涉及查询、退款等通用能力，还需读取 `references/code-examples/nodejs/1-通用接口/` 下对应的代码示例
- [M-03] 已通过 curl 读取接入规范与常见陷阱文档
- [M-03] 已通过 curl 递归读取对应产品文档（快速接入、接口列表、注意事项等）

#### 接入前必读

> ⚠️ **严禁**在未读取下列完整内容的情况下生成代码或给出建议，违反将导致集成失败
- 页面跳转类 API 统一使用 `alipay.trade.page.pay` 和 `pageExec(..., "POST")`，由 SDK 生成已签名的 HTML 表单；不得自行拼接或改写表单字段
- SDK 固定放在项目的 `supabase/functions/_shared/alipay-sdk-deno.ts`；所有 Edge Function 均从 `../_shared/alipay-sdk-deno.ts` 引入
- 私钥来源：沙箱流程只能使用环境准备脚本返回的 `secrets.ALIPAY_WEB_PRIVATE_KEY`；已有凭据流程使用 `ALIPAY_WEB_PRIVATE_KEY`。均禁止自行生成、格式转换或将 `appPrivateKey` 作为替代值
- 前端必须将服务端返回的 `formHtml` 包装为 **Blob URL** 后跳转；禁止将长的签名 URL 直接赋给 `window.location.href`，否则 URL 超长时浏览器可能截断参数并导致验签失败
- 时间戳格式**必须使用 `yyyy-MM-dd HH:mm:ss`**，禁止使用 ISO 格式

**核心必读项**：
> ⚠️ **严禁**在未读取完整内容的情况下生成代码或给出建议，违反将导致集成失败
- **接入规范与常见陷阱**：使用 `curl -sL "https://ideservice.alipay.com/cms/site/0j0kl2"` 阅读接入规范与常见陷阱。
- **产品文档**：通过 curl 访问 **产品文档路由** 中对应的产品在线文档，并递归阅读其中的快速接入、接口列表、注意事项等部分，了解产品特有逻辑。

### 步骤 1.4 集成代码

**前置条件**：已完成步骤 1.3 集成前置步骤，并对接入规范、及通用接口说明有了充分理解。

**完成条件**：
- [M-04] 已通过代码生成前 6 项自检，全部通过
- [M-04] 已为对应支付产品生成服务端（Edge Function）和前端（Blob URL 跳转）完整代码
- [M-04] 异步通知处理代码已包含验签逻辑

#### 实现说明

在充分理解上述强制约束后，参考`references/code-examples/nodejs/` 中的 Node.js 示例进行代码实现

#### 代码生成前自检

**⛔ 输出任何代码之前，必须逐条自检以下 6 项**，全部通过方可输出代码，任何一项不通过必须返回对应步骤重读，**禁止**跳过自检直接输出代码：

1. 环境准备脚本是否返回 `credentials_ready`，或返回 `secret_registration_required` 后 Secret 注册是否明确成功？→ 是 → 继续
2. 若使用本次沙箱，`ALIPAY_WEB_PRIVATE_KEY` 是否仅来自脚本返回的 `secrets.ALIPAY_WEB_PRIVATE_KEY`，从未使用 `appPrivateKey`？→ 是 → 继续
3. 我使用 `pageExec(..., "POST")` 生成支付 HTML，而非 `exec()` 吗？→ 是 → 继续
4. 时间戳格式是 `yyyy-MM-dd HH:mm:ss` 吗？→ 是 → 继续
5. 前端是否将 SDK 返回的 `formHtml` 使用 **Blob URL** 方式跳转（即 `new Blob([html]) → URL.createObjectURL → window.location.href`），而非直接将长的签名 URL 赋给 `window.location.href`，且没有自行拼接或改写 SDK 生成的表单？→ 是 → 继续
6. 沙箱配置中没有任何编造、假数据，且占位符仅用于用户选择自行申领的场景并已明确标注待填字段吗？→ 是 → 继续


### 步骤 1.5 集成后说明

**前置条件**：已完成步骤 1.4 集成代码，并通过自检确认已满足步骤 1.4 的所有强制约束要求。

**完成条件**：
- [M-05] 已向用户明文打印安全红线（第一部分）
- [M-05] 已向用户输出完整的后续指引文案（第二部分）
- [M-05] （仅当环境准备脚本返回 `secret_registration_required` 且 Secret 注册成功时适用）已明文输出全部 6 项沙箱测试凭据：商家账号、商家登录密码、商家支付密码、买家账号、买家登录密码、买家支付密码，缺一不可

**⛔ 完成支付能力集成任务后，必须向用户输出以下内容，不打印则违规**：

**第一部分：安全红线**

以下为支付宝支付接入的安全红线，违反可能导致资金损失或安全事故，**必须明文打印给用户**：

- 私钥禁止存客户端：构造交易数据并签名必须在商家服务端完成，私钥严禁保存在任何客户端中。
- 私钥禁止记日志：私钥不得出现在任何日志中。
- 私钥禁止传公共仓库：私钥不得上传到 GitHub、GitLab 等公共代码仓库。
- 前台支付结果不可信：前台同步跳转结果不可信，必须以支付宝异步通知或调用交易查询接口获取结果为准。
- 未确认不重付：在未确认支付结果前，不得要求用户再次付款，必须先通过异步通知或查询接口确认支付结果。
- 异步通知必须先验签：收到异步通知后必须先验签，确保通知来自支付宝。

**第二部分：后续指引**

> ⛔ 以下内容仅可在步骤 1.4 完成后输出，不能作为环境检测或沙箱流程的替代分支。

- **沙箱环境**：当前为测试环境，仅用于开发调试，不涉及真实资金。正式收款需完成商家入驻和密钥配置，请按以下指引操作：
  - 仅当本次环境准备脚本返回 `secret_registration_required` 并创建了沙箱时，必须从 `sandbox.sandboxAccounts` 原样读取并**明文输出**以下全部 6 项仅限沙箱测试的凭据，**缺一不可**：
    - 商家账号：`sandbox.sandboxAccounts.partner.email`
    - 商家登录密码：`sandbox.sandboxAccounts.partner.logonPassword`
    - 商家支付密码：`sandbox.sandboxAccounts.partner.payPassword`
    - 买家账号：`sandbox.sandboxAccounts.user.email`
    - 买家登录密码：`sandbox.sandboxAccounts.user.logonPassword`
    - 买家支付密码：`sandbox.sandboxAccounts.user.payPassword`

  1. 打开左侧「技能」列表
  2. 点击支付宝支付技能的配置界面中的前往开通来开通 AI 网页应用收款
  3. 开通后将正式的 `APPID`、`应用私钥`、`支付宝公钥` 填入并提交

- **正式环境**：已使用正式密钥集成，上线前请确认已完成 [集成校验清单](references/main/checklist.md) 中的「上线前校验」各项，并确保网关地址已切换为 `https://openapi.alipay.com/gateway.do`。

### 步骤 1.6 集成校验

**完成条件**：
- [M-06] 已获得用户同意并按照集成校验清单逐项校验并输出结果

<BLOCKING_CONFIRMATION>

**确认点：步骤 1.6 集成校验**

输出步骤 1.5 的「安全红线」和「后续指引」后，**必须暂停并等待用户确认**：

1. **输出校验邀请**：
   > "支付能力集成已完成。为确保集成质量，我可按照 [集成校验清单](references/main/checklist.md) 帮您逐项校验（包括签名验签、异步通知、异常处理等）。**请问您是否需要进行集成校验？请回复'需要'或'确认'后**，我将开始校验。"
2. **等待用户回复**：
   - 若用户回复需要校验：**获得明确同意后**，方可按照校验清单逐项执行并输出结果。
   - 若用户表示不需要或无回复：**禁止**主动启动校验流程，可解答用户其他疑问。

</BLOCKING_CONFIRMATION>

在集成过程中及发布上线前按照 [集成校验清单](references/main/checklist.md) 进行校验，确保签名验签、异步通知、异常处理等符合规范。校验结果供参考，开发者务必按照支付宝最新开放平台文档进行检查。

---

## 功能二：集成问题排查指引

**触发条件**：用户在集成支付宝支付产品过程中遇到报错或其他问题。触发关键词："报错"、"错误码"、"问题"、"排查"、"调试"、"异常"等。

执行下述问题排查步骤之前，**必须明确用户当前集成的支付产品**，否则暂停输出并要求用户澄清，**严禁**在支付产品信息缺失时尝试查阅文档或猜测支付产品。

**仅支持的产品**：手机网站支付、电脑网站支付。其他产品请引导用户查阅开放平台在线文档：https://open.alipay.com?form=payskill


### 步骤 2.1 问题识别与分流

根据用户输入判断问题类型，分流到对应排查路径：

```
用户问题
    +-- 验签失败（invalid-signature / 验签出错）  ← 优先匹配，走专项排查
    |       |
    |       └───> 步骤 2.3 常见问题排查（专项排查流程）
    |
    +-- 有明确错误码（如 "ACQ.TRADE_HAS_SUCCESS"、"INVALID_PARAMETER"）
    |       |
    |       └───> 步骤 2.2 错误码排查
    |
    +-- 无明确错误码（如流程疑问、功能异常等）
            |
            └───> 步骤 2.3 常见问题排查
```

### 步骤 2.2 错误码排查

**适用**：用户提供了明确的错误码。

错误码查询前，**必须确认发生报错的接口信息**，否则暂停输出并要求用户澄清，严禁在报错接口信息缺失时尝试查阅文档或猜测报错接口。

#### 排查流程

1. **查公共错误码**：查阅 [公共错误码说明](https://ideservice.alipay.com/cms/site/02km9f)，根据用户提供的错误码检索相关内容。如有匹配结果，**输出排查结论**；否则，**查业务错误码**。

2. **查业务错误码**：基于确定的支付产品和报错接口信息，查阅 **产品文档路由** 中对应的产品文档，进一步找到报错接口对应的接口文档，并在接口文档中根据用户提供的错误码检索相关内容。

3. **输出排查结论**：根据查询到的错误码关联内容，输出排查结论。

### 步骤 2.3 常见问题排查

**适用**：无明确错误码的其他类型问题，或错误码为 `invalid-signature`（验签出错）。

#### 排查流程

1. 根据用户输入和确定的支付产品，查阅对应的产品**常见问题文档**，匹配问题解决方案，回答用户问题时，**务必**以支付宝文档为准。

2. 若根据支付产品**常见问题文档**未找到解决方案，引导用户查阅 [开放平台在线文档](https://open.alipay.com?form=payskill) 或咨询 [支付宝技术支持](https://opensupport.alipay.com/support/intelligent-services?form=payskill)，**严禁**编造常见问题文档以外的解决方案。

#### ⛔ 验签失败（invalid-signature）专项排查

遇到 `invalid-signature`（验签出错）时，**严禁凭猜测归因到私钥格式或环境兼容性**，必须按以下原则排查：

1. **先验证实际请求内容，再怀疑密钥配置** — 验签失败最常见的原因是请求参数不完整或不匹配（如 `return_url` 未传入支付请求），而非私钥格式问题。
2. **严禁的技术推断**：
   - "Node.js 18+ 不支持 PKCS#1" → **错误**，PKCS#1 完全被支持
   - "Node.js 18+ 的 OpenSSL 3.0 不支持 PKCS#1，导致 ERR_OSSL_UNSUPPORTED" → **错误**，OpenSSL 3.0 完全支持 PKCS#1
   - 看到验签失败就怀疑私钥格式 → **错误**，应先检查请求参数完整性
   - "添加 PEM 头尾后仍报错，说明应该转换为 PKCS#8 格式" → **错误**，这是连环误判，添加 PEM 头尾本身就是错误操作，格式转换只会让问题更严重。
3. **正确排查顺序**：先检查请求参数完整性 → 再检查签名字符串构造 → 最后检查密钥配置

#### 常见问题文档索引

| 产品类别 | 常见问题文档 |
| --- | --- |
| 手机/电脑网站支付 | [手机/电脑网站支付常见问题](https://ideservice.alipay.com/cms/site/0j3kh1) |

---

## 产品文档路由

根据用户的业务场景，路由到对应的产品文档：

| 支付产品      | 核心 API | 在线文档                                                         |
| --- | --- | --- |
| 手机/电脑网站支付 | `alipay.trade.page.pay` | [手机/电脑网站支付文档](https://ideservice.alipay.com/cms/site/0iztfv) |
